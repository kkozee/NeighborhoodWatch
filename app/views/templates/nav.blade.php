<nav>
    <ul>
        <li>
            <a href="{{ URL::route('home') }}">Home</a>
            <a href="{{ URL::route('report-incident') }}">Incident Report</a>
            <a href="{{ URL::route('incidents') }}">Incidents</a>
            <a href="{{ URL::route('contact') }}">Emergency Contact</a>
        </li>
    </ul>
</nav>